var backBtn = document.querySelector("#backBtn");
backBtn.addEventListener("click", function() {
  // when you click to go back
  // hide the current question
  // -1 on the current question id
  // -1 on the question counter
  // -1 on the section if necessary
  // check if previous question is excluded
  // show the new current question
  // add a special flag so when user hits submit then the answers are updated, not added to end
})

// TODO: consider how people will reset answers e.g. radios they might want to skip instead
// TODO how to remove answers from a question that's been skipped after storage?

// moveBackward
// prevQuestion

function goBack() {

}
